<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.financeapp">

    <!-- Permissão para escrita no armazenamento (se necessário) -->
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Finance App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity
            android:name=".DefinirMetaActivity"
            android:exported="false" />
        <activity
            android:name=".GraficoPizzaActivity"
            android:exported="false" />
        <activity
            android:name=".EditarTransacaoActivity"
            android:exported="false" />
        <activity
            android:name=".ListaTransacoesActivity"
            android:exported="false" />
        <activity
            android:name=".AdicionarTransacaoActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
